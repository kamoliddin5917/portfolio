const p = document.querySelector(".pppp")

const options = {
    strings: ['Assalomu Alaykum', "Hush kelibsiz!", "Yaratgan saytlarim yoqqan bo'lsa hursandman ☺!"],
    typeSpeed: 70,
    startDelay: 500,
    backSpeed: 40,
    backDelay: 1000,
    loop: true,
    loopCount: Infinity,
    showCursor: false,
  };
  
  const typed = new Typed(p, options);

  const typed1 = new Typed('#h1', {
    strings: ["kamoliddin jamoliddinov web-developer", "full-stack developer"],
    typeSpeed: 100,
    startDelay: 500,
    backSpeed: 40,
    backDelay: 1000,
    loop: true,
    loopCount: Infinity,
    showCursor: false,
  });
  const typed2 = new Typed('#h2', {
    strings: ["HTML", "CSS", "SASS"],
    typeSpeed: 100,
    startDelay: 500,
    backSpeed: 40,
    backDelay: 1000,
    loop: true,
    loopCount: Infinity,
    showCursor: false,
  });
  const typed3 = new Typed('#h3', {
    strings: ["Javascript"],
    typeSpeed: 100,
    startDelay: 500,
    backSpeed: 40,
    backDelay: 1000,
    loop: true,
    loopCount: Infinity,
    showCursor: false,
  });
  const typed4 = new Typed('#h4', {
    strings: ["Reactjs"],
    typeSpeed: 100,
    startDelay: 500,
    backSpeed: 40,
    backDelay: 1000,
    loop: true,
    loopCount: Infinity,
    showCursor: false,
  });
  const typed5 = new Typed('#h5', {
    strings: ["Vuejs"],
    typeSpeed: 100,
    startDelay: 500,
    backSpeed: 40,
    backDelay: 1000,
    loop: true,
    loopCount: Infinity,
    showCursor: false,
  });
  const typed6 = new Typed('#h6', {
    strings: ["Nodejs"],
    typeSpeed: 100,
    startDelay: 500,
    backSpeed: 40,
    backDelay: 1000,
    loop: true,
    loopCount: Infinity,
    showCursor: false,
  });